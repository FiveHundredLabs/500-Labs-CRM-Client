# Browser API calls are intentionally same-origin and relative:
# /api/v1
#
# Local Vite proxies /api to http://localhost:3000.
# Vercel rewrites /api to https://api.500crm.residuesolution.io/api.
#
# Do not configure the browser app to call api.500crm.residuesolution.io directly.
