VITE_API_BASE_URL=/api/v1

# Used by the Vite dev server only. This value is not exposed to browser code.
DEV_API_TARGET=http://localhost:3000

# Production Vercel must also use:
# VITE_API_BASE_URL=/api/v1
#
# Do not configure the browser app to call api.500crm.residuesolution.io directly
# unless intentionally using the optional direct cross-origin mode.
